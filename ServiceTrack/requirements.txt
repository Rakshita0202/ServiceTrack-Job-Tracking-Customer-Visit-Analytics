# For local development and running the unit tests only.
# On Databricks, PySpark and Delta Lake are already provided by the runtime --
# do NOT pip-install pyspark on a Databricks cluster.

pyspark==3.5.1
pytest==8.3.2
delta-spark==3.2.0
